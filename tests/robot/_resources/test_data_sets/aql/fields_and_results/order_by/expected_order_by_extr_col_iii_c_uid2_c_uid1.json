{
	"q": "SELECT ${path} FROM EHR e CONTAINS COMPOSITION c ORDER BY ${path} ${order}",
	"columns": [
		{
			"path": "${path}",
			"name": "#0"
		}
	],
	"rows": [
		[
			"${c_uid2}::${system_id_with_tenant}::1"
		],
		[
			"${c_uid1}::${system_id_with_tenant}::1"
		]
	]
}